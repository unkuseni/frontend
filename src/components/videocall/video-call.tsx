import React, { useEffect, useRef, useState } from "react";
import { useInitiateCall } from "@/hooks/call";
import socket from "@/socket";

const VideoCall: React.FC = () => {

  return (
    <div className="min-h-s">
      video section
    </div>
  );
};

export default VideoCall;
